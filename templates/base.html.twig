<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sortir{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Anton&display=swap" rel="stylesheet">

    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
</head>
<body class="bg-[#F3F4F6] text-black flex flex-col min-h-screen">

    <style> 
        .font-anton {
            font-family: 'Anton', sans-serif;
        }
    </style>

    <!-- Header -->
    <header class="bg-[#2C2C2C] p-3 shadow-md fixed top-0 left-0 w-full z-50">
        <div class="container mx-auto flex justify-between items-center">
            <!-- Logo section -->
            <a href="{{ path('home')}}">
                <div class="flex items-center">
                    <img src="{{ asset('img/logo_sans_text.png') }}" alt="Logo" class="h-12">
                    <h1 class="text-white ml-4 text-3xl font-anton">CAMPUS EXPLORER</h1>
                </div>
            </a>

            <!-- Authentication Section -->
            <div class="flex space-x-4">
                {% if app.user %}
                    <a href="{{ path('home') }}" class="text-gray-200 transition duration-300 ease-in-out transform hover:scale-105">Accueil</a>
                    <a href="{{ path('app_profile') }}" class="text-gray-200 transition duration-300 ease-in-out transform hover:scale-105">Mon profil</a>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('admin_dashboard') }}" class="text-gray-200 transition duration-300 ease-in-out transform hover:scale-105">Administration</a>
                {% endif %}

                {% if app.user %}
                    <a href="{{ path('logout') }}" class="text-gray-200 transition duration-300 ease-in-out transform hover:scale-105">Se déconnecter</a>
                {% endif %}
            </div>
        </div>
    </header>

    <!-- Main Content Block -->
    <main class="container mx-auto p-4 pt-20 flex-grow">
        {% block body %}{% endblock %}
    </main>

 <!-- Footer -->
<footer class="bg-gray-800 text-white p-1 text-center flex items-center justify-center z-50">
  <p class="p-4">@By CAMPUS EXPLORER</p>
    <img src="{{ asset('img/logo_eni.png') }}" alt="Logo" class="">
  
</footer>



</body>
</html>
