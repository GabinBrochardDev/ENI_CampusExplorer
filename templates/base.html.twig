<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sortir{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
</head>
<body class="bg-[#F3F4F6] text-black">

    <!-- Header -->
    <header class="bg-[#2C2C2C] p-6 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <!-- Logo section -->
            <div class="flex items-center bg-white">
                <img src="{{ asset('img/logo_eni.png') }}" alt="Logo" class="h-8">
            </div>

            <!-- Navigation Menu -->
            <nav class="flex space-x-8">
                {# <a href="{{ path('product') }}" class="text-[#1E3A8A] hover:text-blue-500">Product</a>
                <a href="{{ path('features') }}" class="text-[#1E3A8A] hover:text-blue-500">Features</a>
                <a href="{{ path('marketplace') }}" class="text-[#1E3A8A] hover:text-blue-500">Marketplace</a>
                <a href="{{ path('company') }}" class="text-[#1E3A8A] hover:text-blue-500">Company</a> #}
            </nav>

            <!-- Authentication Section -->
            <div class="flex space-x-4">
                
                {% if app.user %}
                    <a href="{{ path('home') }}" class="text-gray-200">Accueil</a>
                    <a href="{{ path('app_profile') }}" class="text-gray-200 ">Mon profil</a>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('admin_dashboard') }}" class=" text-gray-200 ">Administration</a>
                {% endif %}

                {% if app.user %}
                    <a href="{{ path('logout') }}" class=" text-gray-200">Se déconnecter</a>
                {% endif %}
            </div>
        </div>
    </header>

    <!-- Main Content Block -->
    <main class="container mx-auto p-4 ">
        {% block body %}{% endblock %}
    </main>

</body>
</html>
