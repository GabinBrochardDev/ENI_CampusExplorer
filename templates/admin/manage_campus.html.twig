{% extends 'base.html.twig' %}

{% block title %}Gérer les campus{% endblock %}

{% block body %}
    <h1 class="text-3xl font-bold mb-6 text-center text-gray-800">Gérer les campus</h1>

    <!-- Formulaire de recherche -->
    <div class="flex justify-center mb-6">
        {{ form_start(form, { 'attr': { 'class': 'flex items-center w-full max-w-xl' } }) }}

        <!-- Champ de recherche avec le bouton fusionné -->
        <div class="flex w-full max-w-xl">
            <!-- Champ de recherche -->
            {{ form_widget(form.nom, { 'attr': { 
                'class': 'w-full p-3 rounded-l-md border border-gray-300 focus:ring focus:ring-blue-300 focus:border-blue-500 transition duration-200', 
                'placeholder': 'Rechercher un campus' 
            } }) }}

            <!-- Bouton de recherche, fusionné avec le champ -->
            <button type="submit" class="bg-blue-600 text-white px-6 py-3 rounded-r-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-300 transition duration-200">
                Rechercher
            </button>
        </div>

        {{ form_end(form) }}
    </div>

    <!-- Tableau des campus, ajusté pour être plus petit et centré -->
    <div class="overflow-x-auto max-w-1xl mx-auto text-center">
        <table class="table-auto w-full min-w-full border border-gray-300 shadow-lg">
            <thead class="bg-blue-50">
                <tr>
                    <th class="px-4 py-2 text-left text-gray-700 font-semibold text-center">Campus</th>
                    <th class="px-4 py-2 text-left text-gray-700 font-semibold text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for campus in campusList %}
                    <tr class="{% if loop.index is odd %}bg-white{% else %}bg-gray-50{% endif %} border-t hover:bg-gray-100 transition-colors">
                        <td class="px-4 py-2 text-gray-800">{{ campus.nom }}</td>
                        <td class="px-4 py-2">
                            <a href="{{ path('admin_edit_campus', {id: campus.id}) }}" class="text-blue-600 hover:text-blue-800 transition-colors">Modifier</a>
                            <a href="{{ path('admin_delete_campus', {id: campus.id}) }}" class="text-red-600 hover:text-red-800 transition-colors ml-4">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
                <tr class="bg-gray-100 border-t">
                    <td colspan="2" class="text-center py-4">
                        <a href="{{ path('admin_add_campus') }}" class="text-blue-600 hover:text-blue-800 font-semibold hover:underline">Ajouter</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
{% endblock %}
